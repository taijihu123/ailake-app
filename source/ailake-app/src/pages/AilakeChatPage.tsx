import React from 'react';
import AilakeChat from '../components/AilakeChat';

const AilakeChatPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-green-50 to-white">
      <AilakeChat />
    </div>
  );
};

export default AilakeChatPage;